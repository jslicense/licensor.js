module.exports = function(transform, argument) {
  // if (transform) {
  //   if (transform === 'upper-case') {
  //     return argument.toUpperCase();
  //   } else {
  //     throw new Error('Unknown transform "' + transform + '"');
  //   }
  // } else {
  return argument;
  // }
};
